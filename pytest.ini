[pytest]
pythonpath = back
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::ResourceWarning
markers =
    e2e: end-to-end tests with real chat flows and report generation
# Покрытие и порог 70%: pytest --cov=src --cov-fail-under=70 --cov-report=term-missing
